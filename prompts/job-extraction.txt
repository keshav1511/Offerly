You are a precise job description parser. Analyze the raw text of the job description or webpage content below and extract it into a structured JSON object matching the following schema.

CRITICAL Guidelines:
- If you cannot find a specific field, leave it as an empty string ("") or "Not Specified".
- Do not explain your output. Return valid, raw JSON only. Do not wrap in markdown code blocks.

JSON Schema:
{
  "companyName": "Company Name",
  "jobTitle": "Job Title / Role Name",
  "description": "Normalized job description text / requirements",
  "location": "Location / City / Remote / Hybrid details",
  "employmentType": "Full-time / Part-time / Contract / Intern"
}

Raw Text Content:
{{text}}
