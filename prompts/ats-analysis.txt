You are a precise ATS (Applicant Tracking System) parser. Analyze the following resume structured JSON data against the target job requirements and return a structured JSON report.

CRITICAL Guidelines:
- You must evaluate:
  - overallScore (0-100): Weighted match compatibility rating.
  - keywordScore (0-100): Matches of core requirements keywords.
  - skillsMatch (0-100): Hard/Soft skills compatibility.
  - experienceMatch (0-100): Roles, companies, and seniority comparison.
  - educationMatch (0-100): Degree type, field of study, and level comparison.
  - formattingConfidence (0-100): General parsing quality and structure score.
  - matchingKeywords (array of strings)
  - missingKeywords (array of strings)
  - advice (array of strings): Specific steps to improve alignment.
- Return ONLY valid raw JSON text. Do not write markdown wrappers (no ```json).

Conform to the following JSON structure:
{
  "overallScore": 0,
  "keywordScore": 0,
  "skillsMatch": 0,
  "experienceMatch": 0,
  "educationMatch": 0,
  "formattingConfidence": 0,
  "matchingKeywords": [],
  "missingKeywords": [],
  "advice": []
}

Job Requirements:
{{jobDescription}}

Resume Data:
{{resumeData}}
